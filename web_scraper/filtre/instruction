se placer dans :
.../filtre/filtre

entrer la commande :

scrapy crawl Filtre -a urls=$(pwd)/urls -a rayons=$(pwd)/rayons -a params='{"auchan": { "nbrayons": 2, "selecteur": "nav.site-breadcrumb__nav a::text"}, "franprix": { "nbrayons": 3, "selecteur": "ul.breadcrumb.mobile-container-x.mb-6 li a::text"} }'  -O output/result.jl

container docker:
  docker build -t webscraping/scrapy:1.0 .
  docker run --name scrap -d webscraping/scrapy:1.0
  docker exec scrap /filtre/filtre/start.sh